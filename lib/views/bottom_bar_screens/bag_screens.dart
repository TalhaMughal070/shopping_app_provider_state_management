import 'package:flutter/material.dart';

class Bag_Screen extends StatelessWidget {
  const Bag_Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Bag screen')),
    );
  }
}
